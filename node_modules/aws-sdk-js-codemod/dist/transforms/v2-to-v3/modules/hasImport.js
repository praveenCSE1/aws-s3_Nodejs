"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.hasImport = void 0;
const config_1 = require("../config");
const hasImport = (j, source) => source
    .find(j.ImportDeclaration)
    .filter((importDeclaration) => {
    const { value: sourceValue } = importDeclaration.value.source;
    return typeof sourceValue === "string" && sourceValue.startsWith(config_1.PACKAGE_NAME);
})
    .size() > 0;
exports.hasImport = hasImport;
